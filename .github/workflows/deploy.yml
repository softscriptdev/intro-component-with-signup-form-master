name: Deploy App

on:
  push:
    branches:
      - main

jobs:
  call-deploy:
    uses: softscriptdev/GitHub-Actions-to-deploy-vite/.github/workflows/deploy.yml@main
    with:
      base-path: "/intro-component-with-signup-form-master/"